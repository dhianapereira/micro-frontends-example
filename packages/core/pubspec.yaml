name: core
description: "The package responsible for connecting the micro_apps to the base_app"
version: 0.0.1

environment:
  sdk: '>=3.2.4 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
    
  flutter_lints: 2.0.0
  mocktail: 1.0.3

flutter: