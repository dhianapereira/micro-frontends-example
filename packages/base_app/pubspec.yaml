name: base_app
description: "The package responsible for initializing the application"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.4 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  dependencies:
    path: ../dependencies
  core:
    path: ../core
  micro_app_login:
    path: ../micro_apps/micro_app_login
  micro_app_home:
    path: ../micro_apps/micro_app_home

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:

  uses-material-design: true
  