name: foundations
description: Shared contracts and primitives for the micro frontends workspace.
version: 0.0.1
publish_to: 'none'

environment:
  sdk: '>=3.12.0 <4.0.0'
  flutter: ">=3.44.0"

resolution: workspace

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: 6.0.0

flutter:
